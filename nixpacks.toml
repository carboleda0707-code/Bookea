[phases.setup]
nixPkgs = ["python3", "gcc"]

[phases.start]
cmd = "uvicorn app.main:app --host 127.0.0.1 --port 8000 & streamlit run app/frontend/frontend.py --server.port $PORT --server.address 0.0.0.0 --server.headless true --server.enableCORS false"